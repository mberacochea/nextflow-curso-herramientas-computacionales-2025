/*
 * Pipeline configuration
 */

params {
    // Input/output options
    reference = null
    reads = null
}

profiles {
    docker {
        docker.enabled = true
    }

    conda {
        conda.enabled = true
    }
}
